// Intentionally stale fixture for `npm run contract:check`.
// The check must fail against this file and pass only because --expect-stale is set.
export declare const paperclipOpenApiContract: {
  readonly openapi: "3.0.0";
  readonly paths: {};
};
